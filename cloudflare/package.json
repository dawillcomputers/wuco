{
  "name": "wuco-cloudflare",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply WEA_DB --local",
    "db:migrate:remote": "wrangler d1 migrations apply WEA_DB --remote"
  },
  "devDependencies": {
    "wrangler": "^4.119.0"
  }
}
